<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>{% block title %}Welcome!{% endblock %}</title>
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <!-- Favicon-->
    <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
    {% block stylesheets %} {% endblock %} {% block javascripts %} {% endblock
    %}
  </head>

  <header>
    {% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="{{ path('app_all_product') }}"
          >Start Bootstrap</a
        >
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
            <li class="nav-item">
              <a
                class="nav-link active"
                aria-current="page"
                href="{{ path('app_all_product') }}"
                >Home</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_all_user') }}">users</a>
            </li>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li>
                  <a class="dropdown-item" href="{{ path('app_all_product') }}"
                    >All Products</a
                  >
                </li>
                <li><hr class="dropdown-divider" /></li>
                <li><a class="dropdown-item" href="#!">Popular Items</a></li>
                <li><a class="dropdown-item" href="#!">New Arrivals</a></li>
              </ul>
            </li>
          </ul>
          <form class="d-flex">
            <button
              class="btn btn-outline-dark"
              type="submit"
              style="margin-right: 3rem"
            >
              <a class="dropdown-item" href="{{ path('app_add_annonce') }}"
                >add annonce</a
              >
            </button>
            {# Vérifier si l'utilisateur est connecté #} 
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %} {# L'utilisateur est connecté, afficher le bouton "Log out" #}
            <button class="btn btn-outline-dark" type="submit">
              <a class="dropdown-item" href="{{ path('app_logout') }}"
                >Log out</a
              >
            </button>
            {% else %} {# L'utilisateur n'est pas connecté, afficher le bouton
            "Log in" ou "Register" #}
            <button
              class="btn btn-outline-dark"
              style="margin-right: 1rem"
              type="submit"
            >
              <a
                class="dropdown-item"
                href="{{ path('app_form_registration') }}"
                >Register</a
              >
            </button>
            <button class="btn btn-outline-dark" type="submit">
              <a class="dropdown-item" href="{{ path('app_login') }}">Log in</a>
            </button>
            {% endif %}
          </form>
        </div>
      </div>
    </nav>
    {% endblock %} {% block header %}
    <header class="bg-dark py-5 mb-5">
      <div class="container px-4 px-lg-5 my-5">
        <div class="text-center text-white">
          <h1 class="display-4 fw-bolder">Shop in style</h1>
          <p class="lead fw-normal text-white-50 mb-0">
            With this shop hompeage template
          </p>
        </div>
      </div>
    </header>
    {% endblock %}
  </header>
  <body>
    {% block body %}{% endblock %}
  </body>
  <footer>
    {% block footer %}
    <!-- Footer-->
    <footer class="py-5 bg-dark">
      <div class="container">
        <p class="m-0 text-center text-white">
          Copyright &copy; Your Website 2023
        </p>
      </div>
    </footer>
    {% endblock %}
  </footer>
</html>
